<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="forecast"
            type="com.diskin.alon.pagoda.weatherinfo.appservices.model.HourForecastDto" />
    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/hour"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="17:00"
            app:setHour="@{forecast.hour}"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:layout_margin="16dp"
            android:layout_gravity="center_horizontal"/>

        <ImageView
            android:id="@+id/hourWeatherIcon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center_horizontal"
            app:setWeatherIcon="@{forecast.condition}"
            tools:srcCompat="@drawable/ic_weather_clear_night_96"/>

        <TextView
            android:id="@+id/temp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:layout_gravity="center_horizontal"
            android:layout_margin="16dp"
            app:setTemp="@{forecast.temp}"
            tools:text="22&#xb0;" />

    </LinearLayout>
</layout>